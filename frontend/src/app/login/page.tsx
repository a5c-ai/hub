import { LoginForm } from '@/components/forms/LoginForm';

export default function LoginPage() {
  return <LoginForm />;
}

export const metadata = {
  title: 'Sign In - Hub',
  description: 'Sign in to your Hub account',
};