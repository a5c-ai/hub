import { RegisterForm } from '@/components/forms/RegisterForm';

export default function RegisterPage() {
  return <RegisterForm />;
}

export const metadata = {
  title: 'Sign Up - Hub',
  description: 'Create a new Hub account',
};